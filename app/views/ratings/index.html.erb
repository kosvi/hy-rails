<h1>Ratings</h1>

<%= render "top", top: @top_beers, type: "Beer" %>

<%= render "top", top: @top_breweries, type: "Brewery" %>

<h2>Recent ratings</h2>

<div>
  <ul>
    <% @latest.each do |l| %>
      <li><%= l %></li>
    <% end %>
  </ul>
</div>
